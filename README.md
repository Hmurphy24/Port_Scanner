Port Scanner

This program was created using Python and displays the open ports on a specified IP or device name. The program takes in the range of ports to scan as user input and displays which ports in the range are currently open. 
